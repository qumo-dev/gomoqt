// Package transport exposes transport stream/connection abstractions and
// transport-layer error types shared across QUIC and WebTransport integrations.
//
// This package is the canonical home for transport contracts and error aliases.
package transport
