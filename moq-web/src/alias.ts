/** Numeric priority of a track. Lower values indicate higher priority. */
export type TrackPriority = number;
/** Unique identifier assigned to each subscription within a session. */
export type SubscribeID = number;

/** Human-readable name identifying a track within a broadcast. */
export type TrackName = string;

/** Zero-based monotonically increasing sequence number of a group. */
export type GroupSequence = number;
